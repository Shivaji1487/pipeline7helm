apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline7helm-pvc
  namespace: abc-bank
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: pipeline7helm-pv
  storageClassName: manual