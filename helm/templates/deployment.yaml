apiVersion: apps/v1
kind: Deployment
metadata:
  name: pipeline6-app
  namespace: abc-bank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pipeline6-app
  template:
    metadata:
      labels:
        app: pipeline6-app
    spec:

      initContainers:
      - name: copy-html
        image: sshivaji555/pipeline6-app:latest
        imagePullPolicy: Always
        command:
          - sh
          - -c
          - cp -r /usr/share/nginx/html/* /pv/
        volumeMounts:
          - name: html-volume
            mountPath: /pv

      containers:
      - name: nginx
        image: sshivaji555/pipeline6-app:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 80
        volumeMounts:
          - name: html-volume
            mountPath: /usr/share/nginx/html

      volumes:
      - name: html-volume
        persistentVolumeClaim:
          claimName: pipeline6-pvc
